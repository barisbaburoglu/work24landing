services:
  landing:
    image: work24-landing:latest
    container_name: work24-landing
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${LANDING_HOST_PORT:-18082}:80"
